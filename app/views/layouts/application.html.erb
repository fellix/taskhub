<!DOCTYPE html>
<html>
<head>
  <title>Takshub</title>
  <%= stylesheet_link_tag :all %>
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
</head>
<body>

<% flash.each do |key, value| %>
  <p class="<%= key %>"><%= value %></p>
<% end %>

<%= yield %>

</body>
</html>
